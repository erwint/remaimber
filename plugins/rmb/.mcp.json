{
  "mcpServers": {
    "remaimber": {
      "command": "sh",
      "args": [
        "-c",
        "PATH=\"$HOME/.local/bin:$HOME/bin:$PATH\"; export PATH; command -v remaimber >/dev/null 2>&1 || bash \"${PLUGIN_ROOT}/scripts/ensure-installed.sh\" >&2 2>&1; exec remaimber mcp"
      ]
    }
  }
}
